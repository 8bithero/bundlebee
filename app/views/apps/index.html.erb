<h1>All available applications <small>(<%= @apps.count %>)</small></h1><br/>
<table id="all-apps" class="table table-striped table-hover">
  <theader>
    <th>ID</th>
    <th>Name</th>
    <th>Bundle ID</th>
    <% if signed_in? %>
      <th>Sandbox</th>
    <% end %>
  </theader>
  
  <% @apps.each do |app| %>
    <tr>
      <td><%= app.id %></td>
      <td><%= app.name %></td>
      <td><%= app.bundle_id %></td>
      <% if signed_in? %>
        <td><%= render('sandbox_form', locals = { :app => app }) if signed_in? %></td>
      <% end %>
    </tr>
  <% end %>
</table>